{
  "name": "gulp_base_repository",
  "version": "1.0.0",
  "description": "クリティカルCSS",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gulp": "^3.9.1"
  }
}
